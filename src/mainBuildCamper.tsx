import React from 'react'
import ReactDOM from 'react-dom/client'
import AppBuildCamper from './AppBuildCamper.tsx'
import './indexBuildCamper.css'

ReactDOM.createRoot(document.getElementById('rootBuildCamper')!).render(
  <React.StrictMode>
    <AppBuildCamper />
  </React.StrictMode>,
)
